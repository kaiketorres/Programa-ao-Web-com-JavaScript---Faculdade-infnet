// Não modificar essas linhas
const numA = 200;
const numB = 110;
const numC = 500;

// Substituir SOMENTE as variáveis (numA, numB e numC), a estrutura da operação deve continuar a mesma!
const resultado = (numB + 10) / ((numC - (2 * 50)) / 2) + ((10 * numA) / 2);

console.log(resultado);
